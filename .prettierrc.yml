# Prettier configuration
overrides:
  - files:
      - "*.java"
    options:
      printWidth: 100
      tabWidth: 2
      useTabs: false
      endOfLine: lf
  - files:
      - "*.md"
    options:
      proseWrap: always
      printWidth: 100
