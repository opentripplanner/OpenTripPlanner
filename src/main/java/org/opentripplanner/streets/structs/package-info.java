/**
 * The FSTStruct subclasses are in their own package because FSTStruct requires fields to be protected or public,
 * but we don't want to accidentally access them without using the generated accessor methods.
 */
package org.opentripplanner.streets.structs;